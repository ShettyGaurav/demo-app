apiVersion: v1
kind: Service 
metadata: 
  name: analytics-engine 
  namespace: gaurav
  labels:
    app: analytics-engine
    tier: backend
spec:
  selector: 
    app: analytics-engine
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
